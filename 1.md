**packages:**
npm i mongoose bcryptjs axios motion next-auth@beta lucide-react @reduxjs/toolkit react-redux leaflet react-leafle @types/leaflet leaflet-geosearch stripe razorpay dotenv @paypal/checkout-js @types/paypal__checkout-js

**Notes:**

1. maine app router use kiya h so avi mai src/app ke andar jo bhi folder banaunga wo route bn jayegi mtlb agar app/Home toh /home route pr wo home page render hoga.

2. Next.js is a server-less mtlb koi ek server pr depend nhi rhta h mtlb ye page load pr depend krta h jb jb page load hoga tb api call hota h. app ka mtlb hota h slash (/). src/app/api/auth/register/route.ts _means_ /api/auth/register.

3. [...nextauth] is a dynamic route which means it can be anything.
    http://localhost:3000/api/auth/session

4. Next.js se sare components server components hote h but it gives us one feature that hmlog alag se client component bnaye, aur server component ke andar client component ko use kr skte h import kr skte h.

---

src/lib/
│
├── client/
│ └── (UI helpers, formatters, hooks)
│
├── server/
│ └── (db, auth, cloudinary, api helpers)
│
└── utils/
└── price.ts ← backend & neutral logic

------------=======------------==========-----------===========----------=============-----------

## todos:

1. product ke category ke hisab se units select feature kaise kr skte h avi sare category me sare units aate h but aesa nhi hota na ya hota h

2. Blinkit / Zepto style schema with usme variant support and also dynamic pricing


------------------------------------------

**Next.js start cmd**
npm run dev

**Socket.io start cmd**
node socket-server.js
npm run socket 

<!-- hi -->